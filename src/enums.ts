export enum ProviderScope {
  Singleton = 'Singleton',
  Transient = 'Transient',
  Deferred = 'Deferred',
  DeferredTransient = 'DeferredTransient',
}

export enum ProviderType {
  Class = 'Class',
  Value = 'Value',
}
